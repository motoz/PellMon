[conf]
# Read all *.conf files found in and below the "config_dir" directory
config_dir = @confdir@/conf.d

# Full path to the logfile
logfile = @localstatedir@/log/pellmonsrv/pellmon.log

# logging level = info/debug
loglevel = info

